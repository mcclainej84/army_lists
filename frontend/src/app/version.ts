// Fuente única de la versión visible de la app (se muestra en el header).
//
// Convención acordada: empezamos en 0.1 y el decimal sube de 1 en 1 con cada
// tanda de cambios (0.1 -> 0.2 -> 0.3 ... -> 0.10 -> 0.11 ...) sin pasar nunca
// a 1.0 hasta que se decida explícitamente dar el salto.
//
// Actualiza APP_LAST_UPDATED (formato AAAA-MM-DD) cada vez que subas APP_VERSION.
export const APP_VERSION = '0.37';
export const APP_LAST_UPDATED = '2026-07-08';

// Sustituye esta URL por la de tu API desplegada (Render/Railway/Fly...) antes de hacer build de producción.
export const environment = {
  production: true,
  apiBaseUrl: 'https://TU-API-DESPLEGADA.example.com/api',
};
